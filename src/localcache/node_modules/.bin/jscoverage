#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jscoverage/bin/jscoverage" "$@"
  ret=$?
else 
  node  "$basedir/../jscoverage/bin/jscoverage" "$@"
  ret=$?
fi
exit $ret
